# 当前任务卡

- 状态：executing
- 当前任务：将当前分支 `更新天使动画资源` 上的现有改动提交并 push，备注为 `处理家具与桌宠的遮挡关系`。
- 本轮要做：
  - 提交当前工作树里的相关改动。
  - 提交备注使用 `处理家具与桌宠的遮挡关系`。
  - push 到当前远端分支。
- 本轮明确不做：
  - 不开始新的功能修改。
  - 不把编辑器生成的 `.lscache` 临时文件混入提交。
- 完成标准：当前改动已形成一次提交并成功 push 到分支 `更新天使动画资源`。
- 直接相关文件：
  - `Assets/_Project/**`
  - `docs/current-task-card.json`
  - `docs/current-task-card.md`
- 风险与注意事项：需要避免把未跟踪的 `.lscache` 临时文件加入提交。
